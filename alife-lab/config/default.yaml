# config/default.yaml — runtime configuration for the Hybrid Primordial Matrix.
#
# Keys are split automatically between core.environment.EnvironmentConfig
# (the physics of the universe) and core.initializer.PrimordialMatrixConfig
# (how the universe is first populated). See core/initializer.py:_split_config.

# --- Environment physics --------------------------------------------------
width: 256
height: 256
baseline_resource: 24.0
max_resource: 120.0
diffusion_rate: 0.15
regen_rate: 0.06
initial_energy: 400.0
offspring_energy_share: 0.4
mutation_rate: 0.0025
probe_spawn_count: 4
probe_genome_length: 24
local_search_radius: 12
senescence_rate: 0.00005
task_bonus_energy: 15.0

# --- Primordial matrix seeding ---------------------------------------------
noise_fraction: 0.5
seed_instances: 16
random_seed: 1729
